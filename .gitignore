# Local runtime config — copy config.example.yaml -> config.yaml.
# Secrets live in secrets.env; wizard save keeps timestamped backups.
config.yaml
config.yaml.bak-*
secrets.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Models / voices (large binaries, fetched per-device)
*.ppn
*.onnx
*.onnx.json
models/
voices/

# Test / tooling
.pytest_cache/
.coverage
htmlcov/

# OS
.DS_Store

# Claude Code local settings
.claude/
